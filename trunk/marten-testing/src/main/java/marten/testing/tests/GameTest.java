package marten.testing.tests;

import marten.age.util.AgeApp;

public class GameTest extends AgeApp {
    
    /* 
     * This is a sandbox test for game creation
     * TODO:
     * Loading system
     * Menu system (keyboard control + mouse hit)
     * Game parameters system
     * Actual game level loading
     * Change AgeApp to MartenApp (one main loop only)
     */

    @Override
    protected void init() {
	// load menu
    }

    @Override
    protected void mainLoop() {
	// display menu
	// or display game
    }

    @Override
    protected void finalize() {
	
    }
}
